# Endpoints y JSON de prueba - FaceLit Backend

### BASE URL

```
http://localhost:8080
```

---

## 1. Módulo: Gestión de Horarios

### 1.1 Registrar Horario

**Endpoint**

```http
POST /api/admin/schedules
```

**Headers**

```text
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
Content-Type: application/json
```

**JSON**

```json
{
  "idChip": "UUID_DE_LA_FICHA",
  "idEnvironment": "UUID_DEL_AMBIENTE",
  "idInstructor": "UUID_DEL_INSTRUCTOR",
  "dayOfWeek": "LUNES",
  "startTime": "08:00:00",
  "endTime": "10:00:00"
}
```

**Respuesta esperada**

HTTP STATUS

```text
201 CREATED
```

Response

```json
{
  "idSchedule": "uuid-generado",
  "chipName": "Ficha ADSO 2025",
  "environmentName": "Ambiente 101",
  "instructorName": "Carlos Gomez",
  "dayOfWeek": "LUNES",
  "startTime": "08:00:00",
  "endTime": "10:00:00",
  "status": "ACTIVE",
  "message": "Horario registrado correctamente"
}
```

---

### 1.2 Actualizar Horario

**Endpoint**

```http
PUT /api/admin/schedules/{UUID_DEL_HORARIO}
```

**Headers**

```text
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
Content-Type: application/json
```

**JSON**

```json
{
  "idChip": "UUID_DE_LA_FICHA",
  "idEnvironment": "UUID_NUEVO_AMBIENTE",
  "idInstructor": "UUID_NUEVO_INSTRUCTOR",
  "dayOfWeek": "LUNES",
  "startTime": "10:00:00",
  "endTime": "12:00:00"
}
```

**Respuesta esperada**

HTTP STATUS

```text
200 OK
```

Response

```json
{
  "idSchedule": "uuid-del-horario",
  "chipName": "Ficha ADSO 2025",
  "environmentName": "Ambiente 102",
  "instructorName": "Ana Torres",
  "dayOfWeek": "LUNES",
  "startTime": "10:00:00",
  "endTime": "12:00:00",
  "status": "ACTIVE",
  "message": "Horario actualizado correctamente"
}
```

---

### 1.3 Eliminar Horario

**Endpoint**

```http
DELETE /api/admin/schedules/{UUID_DEL_HORARIO}
```

**Headers**

```text
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
```

**Respuesta esperada**

HTTP STATUS

```text
204 NO CONTENT
```

Sin body.

**Posibles respuestas de error**

Horario no encontrado

```json
{
  "message": "Horario no encontrado"
}
```

Horario ya inactivo

```json
{
  "message": "El horario ya está inactivo"
}
```

---

### 1.4 Consultar Horarios de una Ficha

**Endpoint**

```http
GET /api/admin/schedules/chip/{UUID_DE_LA_FICHA}
```

**Headers**

```text
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
```

**Respuesta esperada**

HTTP STATUS

```text
200 OK
```

Response

```json
[
  {
    "idSchedule": "uuid",
    "chipName": "Ficha ADSO 2025",
    "environmentName": "Ambiente 101",
    "instructorName": "Carlos Gomez",
    "dayOfWeek": "LUNES",
    "startTime": "08:00:00",
    "endTime": "10:00:00",
    "status": "ACTIVE"
  }
]
```

---

### 1.5 Consultar Todos los Horarios

**Endpoint**

```http
GET /api/admin/schedules
```

**Headers**

```text
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
```

**Respuesta esperada**

HTTP STATUS

```text
200 OK
```

Response

```json
[
  {
    "idSchedule": "uuid-1",
    "chipName": "Ficha ADSO 2025",
    "environmentName": "Ambiente 101",
    "instructorName": "Carlos Gomez",
    "dayOfWeek": "LUNES",
    "startTime": "08:00:00",
    "endTime": "10:00:00",
    "status": "ACTIVE"
  },
  {
    "idSchedule": "uuid-2",
    "chipName": "Ficha ADSO 2025",
    "environmentName": "Laboratorio 202",
    "instructorName": "Ana Torres",
    "dayOfWeek": "MARTES",
    "startTime": "14:00:00",
    "endTime": "16:00:00",
    "status": "ACTIVE"
  }
]
```

---

### 1.6 Validaciones de Cruce de Horarios

**1.6.1 Ambiente ocupado en la misma franja**

JSON

```json
{
  "idChip": "UUID_OTRA_FICHA",
  "idEnvironment": "UUID_MISMO_AMBIENTE",
  "idInstructor": "UUID_OTRO_INSTRUCTOR",
  "dayOfWeek": "LUNES",
  "startTime": "09:00:00",
  "endTime": "11:00:00"
}
```

Respuesta esperada

```json
{
  "message": "El ambiente ya está asignado para esta franja"
}
```

**1.6.2 Instructor ocupado en la misma franja**

JSON

```json
{
  "idChip": "UUID_OTRA_FICHA",
  "idEnvironment": "UUID_OTRO_AMBIENTE",
  "idInstructor": "UUID_MISMO_INSTRUCTOR",
  "dayOfWeek": "LUNES",
  "startTime": "09:00:00",
  "endTime": "11:00:00"
}
```

Respuesta esperada

```json
{
  "message": "El instructor ya tiene clase en esta franja"
}
```

**1.6.3 Ficha con horario en la misma franja**

JSON

```json
{
  "idChip": "UUID_MISMA_FICHA",
  "idEnvironment": "UUID_OTRO_AMBIENTE",
  "idInstructor": "UUID_OTRO_INSTRUCTOR",
  "dayOfWeek": "LUNES",
  "startTime": "09:00:00",
  "endTime": "11:00:00"
}
```

Respuesta esperada

```json
{
  "message": "La ficha ya tiene un horario en esta franja"
}
```

---

### 1.7 Otras Validaciones

Ficha inexistente

```json
{
  "message": "Ficha no encontrada"
}
```

Ambiente inexistente

```json
{
  "message": "Ambiente no encontrado"
}
```

Instructor inexistente

```json
{
  "message": "Instructor no encontrado"
}
```

Horario inexistente

```json
{
  "message": "Horario no encontrado"
}
```

Horario ya inactivo

```json
{
  "message": "El horario ya está inactivo"
}
```

---

### 1.8 Resumen de Casos de Prueba

| Caso | Resultado esperado |
|------|---------------------|
| Registrar horario correctamente | 201 CREATED |
| Actualizar horario | 200 OK |
| Eliminar horario | 204 NO CONTENT |
| Consultar horarios de una ficha | 200 OK |
| Consultar todos los horarios | 200 OK |
| Cruce de ambiente | "El ambiente ya está asignado para esta franja" |
| Cruce de instructor | "El instructor ya tiene clase en esta franja" |
| Cruce de ficha | "La ficha ya tiene un horario en esta franja" |
| Horario inexistente | "Horario no encontrado" |
| Ficha inexistente | "Ficha no encontrada" |
| Ambiente inexistente | "Ambiente no encontrado" |
| Instructor inexistente | "Instructor no encontrado" |
| Horario ya inactivo | "El horario ya está inactivo" |
| Sin token | 403 FORBIDDEN |
| Token sin permisos | 403 FORBIDDEN |

---

## 2. Módulo: Horarios (Schedules) — Consulta y Eliminación Permanente (Admin)

### BASE URL

```text
http://localhost:8080
```

### 2.1 Consultar Horario de un Aprendiz (Admin)

**Endpoint**

```http
GET /api/admin/schedules/user/{UUID_DEL_APRENDIZ}
```

**Headers**

```text
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
```

**Respuesta esperada**

HTTP STATUS

```text
200 OK
```

Response

```json
[
  {
    "idSchedule": "f7c2e8d1-9a54-45bb-b68f-1b96d8b73d51",
    "chipName": "Ficha ADSO 2025",
    "environmentName": "Ambiente 101",
    "instructorName": "Carlos Ramírez",
    "dayWeek": "MONDAY",
    "startTime": "06:00:00",
    "endTime": "10:00:00",
    "state": "ACTIVE"
  },
  {
    "idSchedule": "52d74b77-8fd2-4d1d-9bde-5d98f1c2a70a",
    "chipName": "Ficha ADSO 2025",
    "environmentName": "Laboratorio 202",
    "instructorName": "Laura Gómez",
    "dayWeek": "WEDNESDAY",
    "startTime": "08:00:00",
    "endTime": "12:00:00",
    "state": "ACTIVE"
  }
]
```

---

### 2.2 Inactivar Horario

**Endpoint**

```http
DELETE /api/admin/schedules/{UUID_DEL_HORARIO}
```

**Headers**

```text
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
```

**Respuesta esperada**

HTTP STATUS

```text
204 NO CONTENT
```

Sin body.

> Después de esta operación el horario queda con estado **INACTIVE**.

---

### 2.3 Eliminar Permanentemente el Horario

> **Importante:** El horario debe estar previamente inactivo.

**Endpoint**

```http
DELETE /api/admin/schedules/{UUID_DEL_HORARIO}/permanent
```

**Headers**

```text
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
```

**Respuesta esperada**

HTTP STATUS

```text
204 NO CONTENT
```

Sin body.

> El horario es eliminado definitivamente de la base de datos.

---

### 2.4 Validaciones para Probar

| Caso | Respuesta esperada |
|------|--------------------|
| Eliminar permanentemente un horario con estado ACTIVE | `"El horario debe estar inactivo antes de eliminarse permanentemente"` |
| UUID de horario inexistente | `"Horario no encontrado"` |
| Usuario inexistente en la búsqueda | `"Usuario no encontrado"` |
| Aprendiz sin ficha activa | `"No hay horarios disponibles para este usuario"` |
| UUID inválido | `400 Bad Request` |
| Sin token | `403 Forbidden` |
| Token de INSTRUCTOR | `403 Forbidden` |

---

### 2.5 Flujo de Prueba

**Paso 1 - Consultar horario del aprendiz**

```http
GET http://localhost:8080/api/admin/schedules/user/{UUID_DEL_APRENDIZ}
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
```

Resultado esperado

```text
200 OK
```

Se obtiene el listado de horarios asociados al aprendiz.

**Paso 2 - Inactivar horario**

```http
DELETE http://localhost:8080/api/admin/schedules/{UUID_DEL_HORARIO}
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
```

Resultado esperado

```text
204 NO CONTENT
```

Estado:

```text
INACTIVE
```

**Paso 3 - Eliminar permanentemente**

```http
DELETE http://localhost:8080/api/admin/schedules/{UUID_DEL_HORARIO}/permanent
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
```

Resultado esperado

```text
204 NO CONTENT
```

Resultado:

```text
El horario fue eliminado permanentemente de la base de datos.
```

---

## 3. Módulo: Consulta de Horarios por Rol

### BASE URL

```text
http://localhost:8080
```

### 3.1 Admin / Coordinator - Consultar Horarios de un Ambiente

**Endpoint**

```http
GET /api/admin/schedules/environment/{UUID_DEL_AMBIENTE}
```

**Headers**

```text
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
```

**Respuesta esperada**

HTTP STATUS

```text
200 OK
```

Response

```json
[
  {
    "idSchedule": "uuid",
    "chipName": "Ficha ADSO 2025",
    "environmentName": "Ambiente 101",
    "instructorName": "Carlos Gomez",
    "dayOfWeek": "LUNES",
    "startTime": "08:00:00",
    "endTime": "10:00:00",
    "status": "ACTIVE"
  },
  {
    "idSchedule": "uuid-2",
    "chipName": "Ficha ADSO 2025",
    "environmentName": "Ambiente 101",
    "instructorName": "Ana Torres",
    "dayOfWeek": "MARTES",
    "startTime": "10:00:00",
    "endTime": "12:00:00",
    "status": "ACTIVE"
  }
]
```

---

### 3.2 Instructor - Consultar Mis Horarios

**Endpoint**

```http
GET /api/instructor/schedules/my-schedules
```

**Headers**

```text
Authorization: Bearer TOKEN_DEL_INSTRUCTOR
```

**Respuesta esperada**

HTTP STATUS

```text
200 OK
```

Response

```json
[
  {
    "idSchedule": "uuid",
    "chipName": "Ficha ADSO 2025",
    "environmentName": "Ambiente 101",
    "dayOfWeek": "LUNES",
    "startTime": "08:00:00",
    "endTime": "10:00:00",
    "status": "ACTIVE"
  },
  {
    "idSchedule": "uuid-2",
    "chipName": "Ficha ADSO 2025",
    "environmentName": "Laboratorio 202",
    "dayOfWeek": "MIÉRCOLES",
    "startTime": "14:00:00",
    "endTime": "16:00:00",
    "status": "ACTIVE"
  }
]
```

---

### 3.3 Aprendiz - Consultar Mi Horario

**Endpoint**

```http
GET /api/apprentice/schedules/my-schedule
```

**Headers**

```text
Authorization: Bearer TOKEN_DEL_APPRENTICE
```

**Respuesta esperada**

HTTP STATUS

```text
200 OK
```

Response

```json
[
  {
    "idSchedule": "uuid",
    "chipName": "Ficha ADSO 2025",
    "environmentName": "Ambiente 101",
    "instructorName": "Carlos Gomez",
    "dayOfWeek": "LUNES",
    "startTime": "08:00:00",
    "endTime": "10:00:00",
    "status": "ACTIVE"
  },
  {
    "idSchedule": "uuid-2",
    "chipName": "Ficha ADSO 2025",
    "environmentName": "Laboratorio 202",
    "instructorName": "Ana Torres",
    "dayOfWeek": "MARTES",
    "startTime": "14:00:00",
    "endTime": "16:00:00",
    "status": "ACTIVE"
  }
]
```

---

### 3.4 Validaciones

**3.4.1 Ambiente sin horarios**

HTTP STATUS

```text
200 OK
```

Respuesta esperada

```json
{
  "message": "No hay horarios disponibles para este ambiente"
}
```

**3.4.2 Instructor sin horarios asignados**

HTTP STATUS

```text
200 OK
```

Respuesta esperada

```json
{
  "message": "No hay horarios disponibles para este instructor"
}
```

**3.4.3 Aprendiz sin ficha activa**

HTTP STATUS

```text
200 OK
```

Respuesta esperada

```json
{
  "message": "No hay horarios disponibles para esta ficha"
}
```

**3.4.4 Aprendiz intenta acceder a rutas de Instructor**

Endpoint

```http
GET /api/instructor/schedules/my-schedules
```

HTTP STATUS

```text
403 FORBIDDEN
```

Respuesta esperada

```json
{
  "message": "Access Denied"
}
```

**3.4.5 Instructor intenta acceder a rutas de Administrador**

Endpoint

```http
GET /api/admin/schedules/environment/{UUID_DEL_AMBIENTE}
```

HTTP STATUS

```text
403 FORBIDDEN
```

Respuesta esperada

```json
{
  "message": "Access Denied"
}
```

**3.4.6 Solicitud sin token**

HTTP STATUS

```text
403 FORBIDDEN
```

Respuesta esperada

```json
{
  "message": "Access Denied"
}
```

---

### 3.5 Resumen de Casos de Prueba

| Caso | Resultado esperado |
|------|---------------------|
| Admin consulta horarios de un ambiente | 200 OK |
| Instructor consulta sus horarios | 200 OK |
| Aprendiz consulta su horario | 200 OK |
| Ambiente sin horarios | "No hay horarios disponibles para este ambiente" |
| Instructor sin horarios asignados | "No hay horarios disponibles para este instructor" |
| Aprendiz sin ficha activa | "No hay horarios disponibles para esta ficha" |
| Aprendiz intenta acceder a `/api/instructor/**` | 403 FORBIDDEN |
| Instructor intenta acceder a `/api/admin/**` | 403 FORBIDDEN |
| Solicitud sin token | 403 FORBIDDEN |

---

## 4. Módulo: Excepciones de Horario (Schedule Exceptions)

### BASE URL

```text
http://localhost:8080
```

### 4.1 Registrar Excepción de Horario

**Endpoint**

```http
POST /api/admin/schedule-exceptions
```

**Headers**

```text
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
Content-Type: application/json
```

**JSON**

```json
{
  "idSchedule": "UUID_DEL_HORARIO",
  "idEnvironment": "UUID_DEL_AMBIENTE_ALTERNO",
  "exceptionDate": "2026-07-16",
  "reason": "Mantenimiento del aula principal"
}
```

**Respuesta esperada**

HTTP STATUS

```text
201 CREATED
```

Response

```json
{
  "idScheduleException": "6f8e5a4d-7d1b-4e95-b56d-2c81c85f6d32",
  "idSchedule": "5c8e8fd4-0cb3-4e5d-b76f-df2f4cbe34f9",
  "chipName": "Ficha ADSO 2025",
  "originalEnvironment": "Ambiente 101",
  "alternateEnvironment": "Laboratorio 202",
  "exceptionDate": "2026-07-16",
  "reason": "Mantenimiento del aula principal",
  "status": "ACTIVE",
  "message": "Excepcion registrada correctamente"
}
```

---

### 4.2 Consultar Excepciones de un Horario

**Endpoint**

```http
GET /api/admin/schedule-exceptions/schedule/{UUID_DEL_HORARIO}
```

**Headers**

```text
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
```

**Respuesta esperada**

HTTP STATUS

```text
200 OK
```

Response

```json
[
  {
    "idScheduleException": "6f8e5a4d-7d1b-4e95-b56d-2c81c85f6d32",
    "idSchedule": "5c8e8fd4-0cb3-4e5d-b76f-df2f4cbe34f9",
    "chipName": "Ficha ADSO 2025",
    "originalEnvironment": "Ambiente 101",
    "alternateEnvironment": "Laboratorio 202",
    "exceptionDate": "2026-07-16",
    "reason": "Mantenimiento del aula principal",
    "status": "ACTIVE",
    "message": null
  }
]
```

---

### 4.3 Eliminar Excepción de Horario

> Al eliminar la excepción, el horario vuelve automáticamente a su ambiente original.

**Endpoint**

```http
DELETE /api/admin/schedule-exceptions/{UUID_DE_LA_EXCEPCION}
```

**Headers**

```text
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
```

**Respuesta esperada**

HTTP STATUS

```text
204 NO CONTENT
```

Sin body.

---

### 4.4 Validaciones para Probar (versión completa)

| Caso | Respuesta esperada |
|------|--------------------|
| Excepción duplicada para el mismo horario y fecha | `"Ya existe una excepcion registrada para este horario en esa fecha"` |
| Ambiente alterno igual al ambiente original | `"El ambiente alterno debe ser diferente al ambiente original del horario"` |
| Horario inexistente | `"Horario no encontrado"` |
| Ambiente inexistente | `"Ambiente no encontrado"` |
| Excepción ya inactiva | `"La excepcion ya está inactiva"` |
| UUID de excepción inexistente | `"Excepcion no encontrada"` |
| Fecha inválida | `400 Bad Request` |
| Sin token | `403 Forbidden` |
| Token de INSTRUCTOR | `403 Forbidden` |

---

### 4.5 Validaciones para Probar (primera aparición en el documento original)

> Nota: esta tabla apareció antes en el documento original, junto con el flujo de prueba de 4.6. Se conserva aquí tal cual, sin eliminarla, para no perder contenido.

| Caso | Respuesta esperada |
|------|--------------------|
| Excepción ya inactiva | `"La excepcion ya está inactiva"` |
| Excepción inexistente | `"Excepcion no encontrada"` |
| UUID inválido | `400 Bad Request` |
| Sin token | `403 Forbidden` |
| Token de INSTRUCTOR | `403 Forbidden` |

---

### 4.6 Flujo de Prueba

**Paso 1 - Consultar excepciones del horario**

```http
GET http://localhost:8080/api/admin/schedule-exceptions/schedule/{UUID_DEL_HORARIO}
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
```

Resultado esperado

```text
200 OK
```

Se obtiene la lista de excepciones asociadas al horario.

**Paso 2 - Eliminar excepción**

```http
DELETE http://localhost:8080/api/admin/schedule-exceptions/{UUID_DE_LA_EXCEPCION}
Authorization: Bearer TOKEN_ADMIN_O_COORDINATOR
```

Resultado esperado

```text
204 NO CONTENT
```

Resultado:

```text
La excepción fue eliminada (inactivada) y el horario volvió automáticamente al ambiente original.
```